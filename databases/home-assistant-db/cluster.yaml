apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: awssm-secret
  namespace: home-assistant
spec:
  refreshInterval: 10m
  secretStoreRef:
    name: secretstore-sample
    kind: ClusterSecretStore
  target:
    name: awssm-secret
    creationPolicy: Owner
  data:
    - secretKey: access-key-id
      remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        key: homelab/prod/global/config
        property: access_key_id
    - secretKey: secret-access-key
      remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        key: homelab/prod/global/config
        property: secret_access_key
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: home-assistant-db
  namespace: home-assistant
spec:
  instances: 3

  resources:
    requests:
      memory: 1024Mi

  storage:
    storageClass: longhorn
    size: 10Gi

  monitoring:
    enablePodMonitor: true

  bootstrap:
    initdb:
      database: homeassistant

  backup:
    barmanObjectStore: &barmanObjectStore
      destinationPath: s3://homelab-prod-database-backups/home-assistant
      serverName: home-assistant-db-backup-v1
      s3Credentials:
        accessKeyId:
          name: awssm-secret
          key: access-key-id
        secretAccessKey:
          name: awssm-secret
          key: secret-access-key
      data:
        compression: gzip
      wal:
        compression: gzip
    retentionPolicy: 21d
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: home-assistant-db-backup-v1
  namespace: home-assistant
spec:
  immediate: true
  schedule: "0 0 */6 * * *"
  cluster:
    name: home-assistant-db
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: home-assistant-db-app-push
  namespace: home-assistant
spec:
  updatePolicy: Replace
  deletionPolicy: None
  refreshInterval: 1h
  secretStoreRefs:
    - name: secretstore-sample
      kind: ClusterSecretStore
  selector:
    secret:
      name: home-assistant-db-app
  data:
    - conversionStrategy: None
      match:
        secretKey: password
        remoteRef:
          remoteKey: homelab/prod/databases/home-assistant/credentials
          property: password
      metadata:
        secretPushFormat: string
    - conversionStrategy: None
      match:
        secretKey: username
        remoteRef:
          remoteKey: homelab/prod/databases/home-assistant/credentials
          property: username
      metadata:
        secretPushFormat: string
    - conversionStrategy: None
      match:
        secretKey: dbname
        remoteRef:
          remoteKey: homelab/prod/databases/home-assistant/credentials
          property: dbname
      metadata:
        secretPushFormat: string
    - conversionStrategy: None
      match:
        secretKey: uri
        remoteRef:
          remoteKey: homelab/prod/databases/home-assistant/credentials
          property: uri
      metadata:
        secretPushFormat: string
    - conversionStrategy: None
      match:
        secretKey: jdbc-uri
        remoteRef:
          remoteKey: homelab/prod/databases/home-assistant/credentials
          property: jdbc-uri
      metadata:
        secretPushFormat: string
