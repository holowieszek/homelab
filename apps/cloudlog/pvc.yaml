apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloudlog-data
  namespace: cloudlog
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi